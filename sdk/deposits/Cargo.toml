[package]
name = "deposits"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
bincode = { version = "=2.0.1", default-features = false, features = ["derive", "std"] }
chain-identity = { path = "../chains/identity" }
indexing = { path = "../indexing" }
signer = { path = "../signing/contract" }
sha2.workspace = true
storage = { path = "../storage" }

[dev-dependencies]
storage-rocksdb = { path = "../storage/rocksdb" }
tempfile = "=3.27.0"
tokio = { version = "=1.49.0", features = ["macros", "rt-multi-thread"] }

[lints]
workspace = true
